{
  "name": "thermospark",
  "version": "1.0.0",
  "description": "Urban Thermal — Interactive Urban Heat Island Experience",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix client\" \"npm run dev --prefix server\" --names \"FE,BE\" --prefix-colors \"cyan,yellow\"",
    "install:all": "npm install && npm install --prefix client && npm install --prefix server",
    "build": "npm run build --prefix client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
